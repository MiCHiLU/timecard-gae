%form(name="edit_user" onsubmit="return false;")
  .form-group
    %label{:for => "name"} Name
    %input.form-control(name="name" type="text" ng-model="c.a.user.name" required)/
  -#.form-group
  -#  %label{:for => "language"} Language
  -#  %select.form-control(name="language" ng-model="language" ng-init="language='{{ a.user.language }}'")
  -#    {% for value, label in language_list %}
  -#    {% if value == user.language %}
  -#    %option(value="{{ value }}" selected) {{ label }}
  -#    {% else %}
  -#    %option(value="{{ value }}") {{ label }}
  -#    {% endif %}
  -#    {% endfor %}
  %div
    %button.btn.btn-primary(ng-click="c.a.me_update()" ng-disabled="edit_user.$invalid||edit_user.$pristine") Update
